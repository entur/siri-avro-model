
@namespace("org.entur.avro.realtime.siri.model")
protocol Common {

	import idl "SiriEnums.avdl";

	record FramedVehicleJourneyRefRecord {
		string? dataFrameRef;
		string? datedVehicleJourneyRef;
	}

	record TranslatedStringRecord {
		string? value;
		string? language;
	}
}
